{
  "$id": "https://raw.githubusercontent.com/willfarrell/sast-json-schema/main/example.json",
  "definitions": {
    "allowed": { "type": "integer", "minimum": 0, "maximum": 1 },
    "allowedProperties": {
      "type": "object",
      "unevaluatedProperties": false,
      "properties": {
        "allowed": { "$ref": "#/definitions/allowed" },
        "if": { "$ref": "#/definitions/allowed" },
        "then": { "$ref": "#/definitions/allowed" },
        "else": { "$ref": "#/definitions/allowed" }
      },
      "maxProperties": 4,
      "required": []
    }
  },
  "allOf": [
    {
      "type": "object",
      "unevaluatedProperties": false,
      "properties": {
        "allowed": {
          "$ref": "#/definitions/allowed"
        },
        "if": {
          "$ref": "#/definitions/allowed"
        },
        "then": {
          "$ref": "#/definitions/allowed"
        },
        "else": {
          "$ref": "#/definitions/allowed"
        }
      },
      "maxProperties": 4,
      "required": []
    },
    {
      "type": "object",
      "unevaluatedProperties": false,
      "$ref": "#/definitions/allowedProperties",
      "properties": {},
      "maxProperties": 4,
      "required": [],
      "if": {
        "type": "object",
        "unevaluatedProperties": false,
        "$ref": "#/definitions/allowedProperties",
        "properties": {
          "if": {
            "type": "integer",
            "enum": [1]
          }
        },
        "maxProperties": 4,
        "required": ["if"]
      },
      "then": {
        "type": "object",
        "unevaluatedProperties": false,
        "$ref": "#/definitions/allowedProperties",
        "properties": {
          "then": {
            "type": "integer",
            "enum": [1]
          },
          "else": {
            "type": "integer",
            "enum": [0]
          }
        },
        "maxProperties": 4,
        "required": ["then", "else"]
      },
      "else": {
        "type": "object",
        "unevaluatedProperties": false,
        "$ref": "#/definitions/allowedProperties",
        "properties": {
          "else": {
            "type": "integer",
            "enum": [1]
          },
          "then": {
            "type": "integer",
            "enum": [0]
          }
        },
        "maxProperties": 4,
        "required": ["then", "else"]
      }
    }
  ]
}
